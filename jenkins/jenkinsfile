pipeline {
    agent any

    stages {
        stage('unit test-backend') {
        agent{
           docker{
           image 'snakee/golang-junit:1.21'
            args '-e GOCACHE=/tmp/.cache'
           reuseNode true
           }
           }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }

}